import React from 'react';
import SelectedRep from '../Components/SelectedRep'

const SelectedRepContainer = (props) => {
    return (
        <div className = "SelectedRepContainer">
            <SelectedRep></SelectedRep>
        </div>
    );
}

export default SelectedRepContainer;